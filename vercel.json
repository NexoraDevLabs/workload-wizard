{
  "buildCommand": "pnpm build",
  "devCommand": "pnpm dev",
  "framework": "nextjs",
  "installCommand": "corepack enable && corepack prepare pnpm@10.15.0 --activate && corepack pnpm install --frozen-lockfile"
}
